```vue
<div v-if="books.length === 0" class="empty-state">
      <p class="empty-state-text">No books in your library yet.</p>
      <p class="empty-state-subtext">Add your first book using the form above!</p>
    </div>
```
